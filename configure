#!/bin/bash
################################################################################
#
# Configure script for Pascal-p6
#
# Sets up the complete Pascal-p6 project: determines the host, architecture
# and bit length this machine presents, and restores the working bin and libs
# directories from the matching directory of the hosts tree. The hostinstall
# make target performs the reverse snapshot.
#
################################################################################

#
# Check command exists
#
# Uses the bash "command" built in.
#
function checkexists {

    command -v $1 >/dev/null
    rc=$?
    if [[ $rc != 0 ]] ; then

        echo "*** No $1 was found"

    fi

}

#
# Determine the running host, architecture and bit length, matching the
# makefile's detection.
#
if [ -n "$OS" ] && [ "$OS" = "Windows_NT" ] ; then

    host="windows"

else

    case `uname` in

        Linux)  host="linux" ;;
        Darwin) host="mac" ;;
        *BSD*)  host="bsd" ;;
        *)      host="linux" ;;

    esac

fi

case `uname -m` in

    x86_64|amd64)        arch="x86";   bits="64" ;;
    i386|i486|i586|i686) arch="x86";   bits="32" ;;
    aarch64)             arch="arm";   bits="64" ;;
    armv*)               arch="arm";   bits="32" ;;
    riscv64)             arch="riscv"; bits="64" ;;
    riscv32)             arch="riscv"; bits="32" ;;
    *)                   arch="x86";   bits="64" ;;

esac

#
# Determine if needed programs exist. The only fatal one is grep, because we
# need that to run this script. The rest will impact the running of various
# test and build scripts.
#
checkexists grep
if [[ $rc != 0 ]] ; then

    exit 1

fi

checkexists diff
checkexists sed
checkexists rm
checkexists cp
checkexists mv

checkexists flip > /dev/null

if [[ $rc != 0 ]] ; then

    echo -n "flip does not exist"
    echo $PATH | grep -q "$PWD/bin" || echo -n ", has PATH been set up?"
    echo
    echo "attempting to make it"
    gcc -o ./bin/flip ./source/flip.c
    checkexists flip > /dev/null
    flip=$rc
    if [[ $rc != 0 ]] ; then

        echo "*** Unable to make flip"

    fi

fi
checkexists ls
checkexists zip

#
# Check user arguments
#
for var in "$@"
do

    if [ $var = "--help" ]
    then

        echo "Configure host for Pascal-p6"
        echo
        echo "--32:        Select 32 bit target"
        echo "--64:        Select 64 bit target"
        echo
        exit 0

    elif [ $var = "--32" ]
    then

        bits="32"

    elif [ $var = "--64" ]
    then

        bits="64"

    fi

done

#
# Restore the working binaries and libraries from the hosts tree
#
hostcell="hosts/$host/$arch/bit$bits"
echo "Set up for $host/$arch/bit$bits"

if [ ! -d "$hostcell/bin" ] ; then

    echo "*** No hosts tree entry for $hostcell"
    exit 1

fi

cp $hostcell/bin/* bin 2>/dev/null
cp $hostcell/libs/* libs 2>/dev/null

#
# On a windows host, remove the unix binaries from bin. The repository
# carries the linux tool set in bin (linux is the default setup); on windows
# those extensionless names shadow the .exe names for the bash test scripts
# (bash resolves the exact name first), so every script would exec a foreign
# binary and fail. Scripts are text files and are kept; only ELF format
# binaries are removed. These are tracked files, so git status shows them
# deleted until a configure on a unix host (or git checkout -- bin) restores
# them.
#
if [ "$host" = "windows" ]; then

    for f in bin/*; do

        # an extensionless file with the ELF magic is a foreign unix binary
        if [ -f "$f" ] && [[ "$(basename "$f")" != *.* ]] && \
           [ "$(head -c 4 "$f" 2>/dev/null)" = $'\x7fELF' ]; then

            rm -f "$f"

        fi

    done

fi
rm -f bin/.gitkeep libs/.gitkeep

echo "Configure completed!"
