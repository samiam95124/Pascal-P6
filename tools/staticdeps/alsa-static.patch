--- a/include/global.h	2020-02-19 01:35:39.000000000 -0800
+++ b/include/global.h	2026-06-10 20:05:29.898565566 -0700
@@ -79,7 +79,7 @@
  */
 #define SND_DLSYM_BUILD_VERSION(name, version) \
   static struct snd_dlsym_link __SND_DLSYM_VERSION(snd_dlsym_, name, version); \
-  void __SND_DLSYM_VERSION(snd_dlsym_constructor_, name, version) (void) __attribute__ ((constructor)); \
+  void __SND_DLSYM_VERSION(snd_dlsym_constructor_, name, version) (void) __attribute__ ((constructor (102))); \
   void __SND_DLSYM_VERSION(snd_dlsym_constructor_, name, version) (void) { \
     __SND_DLSYM_VERSION(snd_dlsym_, name, version).next = snd_dlsym_start; \
     __SND_DLSYM_VERSION(snd_dlsym_, name, version).dlsym_name = # name; \
--- a/src/conf.c	2020-02-19 01:35:39.000000000 -0800
+++ b/src/conf.c	2026-06-10 22:03:13.829353694 -0700
@@ -3621,6 +3621,17 @@
 	const char *str;
 	int (*func)(snd_config_t *root, snd_config_t *config, snd_config_t **dst, snd_config_t *private_data) = NULL;
 	int err;
+	int errors = 1;
+
+	/* A hook whose module cannot be loaded is tolerated when the hook
+	   sets 'errors false': in a static build the module is unavailable
+	   by construction, and the hook must degrade as if absent. */
+	err = snd_config_search(config, "errors", &c);
+	if (err >= 0) {
+		errors = snd_config_get_bool(c);
+		if (errors < 0)
+			errors = 1;
+	}
 
 	err = snd_config_search(config, "func", &c);
 	if (err < 0) {
@@ -3680,17 +3691,20 @@
 	func = h ? snd_dlsym(h, func_name, SND_DLSYM_VERSION(SND_CONFIG_DLSYM_VERSION_HOOK)) : NULL;
 	err = 0;
 	if (!h) {
-		SNDERR("Cannot open shared library %s (%s)", lib, errbuf);
-		err = -ENOENT;
+		if (errors) {
+			SNDERR("Cannot open shared library %s (%s)", lib, errbuf);
+			err = -ENOENT;
+		}
 	} else if (!func) {
-		SNDERR("symbol %s is not defined inside %s", func_name, lib);
+		if (errors)
+			SNDERR("symbol %s is not defined inside %s", func_name, lib);
 		snd_dlclose(h);
-		err = -ENXIO;
+		err = errors ? -ENXIO : 0;
 	}
 	_err:
 	if (func_conf)
 		snd_config_delete(func_conf);
-	if (err >= 0) {
+	if (err >= 0 && func) {
 		snd_config_t *nroot;
 		err = func(root, config, &nroot, private_data);
 		if (err < 0)
